<Window x:Class="MyPalClara.App.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clara" Height="700" Width="500"
        Background="#1a1a2e" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="UserBubble" TargetType="Border">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="60,4,12,4"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
        <Style x:Key="AssistantBubble" TargetType="Border">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="12,4,60,4"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0f3460" Padding="16,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Clara" FontSize="18" FontWeight="Bold" Foreground="#e94560"/>
                <TextBlock x:Name="StatusText" Text=" — Connecting..." FontSize="14"
                           Foreground="#a8a8a8" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Chat History -->
        <ScrollViewer Grid.Row="1" x:Name="ChatScroller" VerticalScrollBarVisibility="Auto" Padding="0,8">
            <StackPanel x:Name="ChatPanel" />
        </ScrollViewer>

        <!-- Input Area -->
        <Grid Grid.Row="2" Margin="12,4,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="InputBox" Grid.Column="0"
                     Background="#16213e" Foreground="White"
                     BorderBrush="#0f3460" BorderThickness="1"
                     Padding="12,10" FontSize="14"
                     AcceptsReturn="False"
                     KeyDown="InputBox_KeyDown"
                     VerticalContentAlignment="Center"/>
            <Button x:Name="SendButton" Grid.Column="1" Content="Send"
                    Click="SendButton_Click"
                    Background="#e94560" Foreground="White"
                    BorderThickness="0" Padding="16,10"
                    FontSize="14" Margin="8,0,0,0" Cursor="Hand"/>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#0a0a1a" Padding="12,6">
            <TextBlock x:Name="StatusBar" Text="Disconnected" Foreground="#666" FontSize="11"/>
        </Border>
    </Grid>
</Window>
